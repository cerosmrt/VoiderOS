LIBRARY / DATABASE
------------------
- SQLite metadata DB: store author/title/filepath/line_count for all O/ books
  Lives on NVMe (~/void/library.db), content stays on NTFS (/mnt/data/void_O)
  Powers fast search in F8 and F7 working set queries
  Ingest script: scripts/build_library_db.py (not yet written)

- Gutenberg downloader: update to also write to DB when saving new books

ARCHITECTURE IDEAS (discussed, not decided)
-------------------------------------------
- Oracle as TAB key on F1/F5 instead of dedicated F4/F8 views (user suggested)
  Would free F4 and F8 for other uses

SYSTEM / INFRASTRUCTURE
-----------------------
- Sync proto-voider changes to pkgs/voider-py/ (system Voider still on old code).
  Pending to port from this session: PROTO-banner gate, FxPanel, F7 working-set
  redesign, contextual Tab, ø-marker-stays-highlighted fix. Then nixos-rebuild.
- FxPanel keybinds moved to Super+J/K/L (crt/grain/bw) in nix/modules/voider.nix —
  needs a nixos-rebuild to take effect (user applies it).
- /mnt/data mount: make persistent across boots (currently manual each session)
- proto-voider run command: `proto-voider` (wrapper cd's into proto-voider/ and
  runs watch.py); Super+P does the same. Must run inside the Wayland session.

TAB / INPUT — next up (2026-06-23)
----------------------------------
- Bigger input box in F2 and F3 so the whole line/title is visible. Today when
  the text reaches the box edge it scrolls out of view. Widen the editor (and/or
  shrink font / let it grow) so the full line fits.
- Up with caret at END of line → land at the END of the previous line (not the
  start, which is the standard). Custom up-navigation in F2 (and F3?).

VOID / PERSONAL WRITING SYSTEM
-------------------------------
- Void scanner: on startup, Voider scans /void recursively, indexes all .txt files
  Index includes: file path, chapter titles, book groupings
  F3 shows chapters from all files across entire void
  Source files never modified

- Book compiler: from F3, select a book and trigger export
  Generates: table of contents, PDF, EPUB (optionally HTML/DOCX/Markdown)
  Triggered via keyboard command inside Voider
  Output saved to configurable output directory
  Source files never modified

- TTS local (two modes):
  Mode 1 — on demand: select file or block, keyboard command reads it aloud
  Mode 2 — I mode: reads ~/void/I.txt from random sentence, loops infinitely
  Engine: piper-tts or espeak-ng (free, offline, lightweight)
  Runs in background while Voider is in use

- Website generator: Python script reads /void, generates static HTML site
  One page per chapter, index per book, global index, quotes page, tags page
  Runs on save or triggered manually from Voider
  Single source of truth: the .txt files. Never modified.

- TTS website (godplaystheartist.com):
  On page load, "I" begins playing from random sentence, loops infinitely
  Feels like the book is always being read — visitor tunes in mid-reading
  Source: same I.txt from /void

I / VOID WRITING SYSTEM
-----------------------
- DUPLICATED_ and TRASH_ files in ~/void/I/ need user review in F3 (Ctrl+Delete to discard)
  Files are at the end of I.txt (last group, unmatched section)

- _generate_i_preview() post-flatten: now generates a flat single-group I_preview.txt on every
  startup since all files are in I/ root. Should be fixed to not overwrite or to skip when flat.

UNDO / RENAME (deferred)
------------------------
- Undo/redo: Ctrl+Z (undo) and Ctrl+Shift+Z (redo). Needs an undo stack for
  doc edits in F2 (and ideally F1 voiding, F3 library ops). Not yet scoped.

- Rename-on-navigate (R5): editing an F3 title then navigating/leaving fires
  os.rename with no confirmation. The '0' portal case is already fixed (read-only).
  Still open for NORMAL titles — decide whether to require an explicit rename key
  or a confirmation instead of renaming silently on navigate.

F3 QUEUE (discussed, not yet implemented)
-----------------------------------------
- Feature 3: split doc by '/' lines into separate files
  Ctrl+Shift+F in F2 triggers split; original file kept
  New files named docname_1.txt, docname_2.txt etc. in same folder

- F6: auto-reformat books on load (same reformat applied in F2)

- F7: show book title only — parse/clean filename, display without extension

- Esc = lock screen: log out, show F1 with name + password prompt

- Feature 6: detaching views — never scoped, needs discussion

- F11 help overlay: doesn't reflect any of the new keys added recently
  (Tab is now contextual per view, F7 redesigned, FxPanel on Super+J/K/L, etc.)

- make f4 an actual formatted version of the book: replace linebreaks with punto y seguido, dot separators with punto y aparte, filename por titulo del capitulo. y no se que mas.

DONE 2026-06-23 (this session)
------------------------------
- F7 working set redesigned: manual, uncapped, ∅ empty slots, Tab fills (no dup),
  Shift+Enter adds slot, Ctrl+Delete removes (loaded book stays).
- Contextual Tab: F1→recycle from I/, F2→insert random I/ line / shuffle para
  lines on '.' / shuffle paragraphs on ø, F5→random O/ line, F6 unchanged.
- ø marker stays the ø glyph even when highlighted (was collapsing to '.').
- FxPanel ported back into proto (Super+J/K/L keybinds, pending rebuild).
- PROTO banner now only shows on the dev sandbox (gated by VOIDER_VOID_DIR).