# Local Claude Code agent config (permissions, etc.) — not project code
.claude/

# Test / tooling artifacts
.coverage
**/.coverage
__pycache__/
*.pyc
.pytest_cache/

# proto-voider runtime state (position, active file) — never committed
proto-voider/state.json
